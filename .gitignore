# Build artifacts
orome
packed_experts
src/*.o
src/*.air
src/*.metallib
*.dSYM/

# Experiment state (not committed — each session appends)
results.tsv
status.md
logs/
*.log
bench_err.txt
run.log

# Model weights (too large for git)
model_weights.bin
model_weights.json
expert_index.json
packed_experts/
packed_experts_2bit/
hot_experts_*.json
vocab.bin

# Generated images
progress.png

# Python / uv
__pycache__/
*.pyc
.venv/
uv.lock

# macOS
.DS_Store

.claude
